<h2>Available drones</h2>
{{#each drones}}
   <div>
        <p>Name: {{name}}</p>
        <p>Propellers: {{propellers}}</p>
        <p>Max Speed: {{maxSpeed}}</p>
        <a href="/drones/{{_id}}/edit">Update the Drone</a>
        <form action="/drones/{{_id}}/delete" method="POST">
        <button type="submit">Delete</button>
        </form>
    </div>
    <br><br>
{{/each}}